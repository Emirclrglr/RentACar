@model ResultCarDescriptionDto

<div class="tab-pane fade" id="pills-manufacturer" role="tabpanel" aria-labelledby="pills-manufacturer-tab">
	@if (Model != null)
	{
		<p>@Model.Details</p>
	}
	else
	{
		<p>Araca dair açıklama bulunmuyor.</p>
	}
</div>
