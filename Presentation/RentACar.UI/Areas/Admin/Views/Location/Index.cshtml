@model X.PagedList.IPagedList<ResultLocationDto>

@using X.PagedList.Mvc.Core
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="row d-flex justify-content-center">
	<div class="col-md-8 ">
		<div class="card">
			<div class="card-body">
				<h4 class="card-title mb-4">Fiyatlandırma İşlemleri</h4>

				<div class="table-responsive">
					<table class="table table-hover mb-0">
						<thead>
							<tr class="text-center">
								<th>Fiyatlandırma Türü</th>
								<th width="200">Haritada Görüntüle</th>
								<th width="100">Düzenle</th>
								<th width="100">Sil</th>
							</tr>
						</thead>
						<tbody>
							@foreach (var item in Model)
							{
								<tr class="text-center" style="font-size: 14px">
									<td class="align-middle">@item.LocationName</td>
									<td class="align-middle"><a target="_blank" href="https://www.google.com/maps/place/@item.LocationName" class="rounded-pill btn btn-outline-success"><i class="las la-globe-europe" style="font-size: 20px"></i></a></td>
									<td class="align-middle"><a href="/Admin/Location/UpdateLocation/@item.Id" class="rounded-pill btn btn-outline-primary"><i class="las la-edit" style="font-size: 20px"></i></a></td>
									<td class="align-middle"><a href="/Admin/Location/DeleteLocation/@item.Id" class="rounded-pill btn btn-outline-danger"><i class="las la-trash" style="font-size: 20px"></i></a></td>
								</tr>
							}
						</tbody>
					</table>

				</div>

			</div>

			<div class="d-flex justify-content-between">
				<a href="/Admin/Location/CreateLocation" class="btn btn-outline-primary ml-5 mt-3 px-5" style="height: 36px">+ Yeni Lokasyon Girişi</a>
				<nav aria-label="Pagination" class="d-flex justify-content-end mt-3 mb-3 mr-5" style="font-size: 16px">
					@Html.PagedListPager(Model, page => Url.Action("Index", new { page }),
											new PagedListRenderOptions
					{
						LiElementClasses = new[] { "page-item mr-2" },
						PageClasses = new[] { "page-link" }
					})
				</nav>
			</div>

		</div>

	</div>
</div>

